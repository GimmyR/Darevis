import { Text, View } from "react-native";

const Record = function({ navigation }) {
    return (
        <View>
            <Text>WATCH RECORD HERE</Text>
        </View>
    );
};

export default Record;